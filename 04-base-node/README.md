#Ejemplo Node con Yargs

```
Imprime en consola la tabla de multiplicar
Comandos:
  app listar  Imprime en consola la tabla de multiplicar
  app crear   Genera un archivo con la tabla de multiplicar

Opciones:
      --help     Muestra ayuda                                        [booleano]
  -b, --base                                                         [requerido]
  -l, --limite                                                     [defecto: 10]
```